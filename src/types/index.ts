export interface LOGIN_DATA {
    email: string;
    password: string;
}

export interface REGISTER_DATA {
    username: string;
    email: string;
    password: string;
}

export interface QUERY_MESSAGE {
    sfindId: string;
    des: string;
}